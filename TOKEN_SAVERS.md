# Token-Saving Workflow for debate-furnace

> **For next session:** Start with `CLI_ENTRY.md` instead (optimized for speed). Use this file for deeper workflow guidance.

## Why this document matters

This document exists to keep future CLI sessions productive without wasting context or money. The goal is to make the first useful steps cheap and repeatable, so the work can move forward with less backtracking and less repeated explanation.

A simple target is to reduce the cost of becoming productive from roughly 100K tokens to roughly 12K tokens. That is an 88% reduction in early-session overhead and keeps the work closer to 12% of the available conversation cap.

The same habits that reduce token usage also improve reliability. Clear entry points, documented checks, and a short recovery path make mistakes easier to catch and easier to fix.

## 🚀 Before Asking Vibe

### 1. Verify Deployment (No Tokens)
```bash
./scripts/verify-deploy.sh
```
- Checks live site
- Checks last-modified timestamp  
- Compares local vs GitHub commits
- **Saves: ~500+ tokens** per deployment check

### 2. Quick Grep (No Tokens)
```bash
# Find where HARD STOP RULE is used
grep -r "HARD STOP" src/ api/

# Find all domain prompts
grep -n "systemContext" src/REI.jsx
```

### 3. Test Locally (No Tokens)
```bash
npm test -- --testPathPattern=REI
```

---

## 📝 Commit Message Template (.gitmessage)
```
[domain] Short description of change

Type: fix|feat|docs|refactor

Changes:
- What changed
- Why it changed
- How to verify

Token impact: low|medium|high

Generated by: Mistral Vibe | Manual
```

---

## 💡 Vibe Session Optimization

### DO (Cheap)
- "Edit src/REI.jsx line 657: change X to Y"
- "grep for HARD STOP RULE usage"
- "Show me git diff"
- "Run npm test"

### AVOID (Expensive)
- "Explain my whole codebase" ✗
- "How do I deploy?" ✗ (use ./scripts/verify-deploy.sh)
- "Write me a tutorial" ✗
- "Review all my tests" ✗ (run them locally first)

---

## 🎯 High-Value Vibe Tasks

| Task | Token Cost | ROI |
|------|------------|-----|
| Create REI.test.jsx | High | ✅ Prevents bugs |
| Fix complex logic | Medium | ✅ Saves dev time |
| Architecture review | High | ✅ Critical decisions |
| Code formatting | Low | ❌ Use Prettier instead |
| Deployment checks | Zero | ❌ Use verify-deploy.sh |

---

## 🔧 Tools Installed

- **scripts/verify-deploy.sh** — Deployment status check
- **.git/hooks/pre-commit** — Secret blocking, deployment reminder

---

## 📊 Token Tracking

Check your usage:
- Vercel: https://vercel.com/account/tokens
- Set budget alerts in Vercel dashboard

**Pro tip:** Short, specific sessions. Batch changes. Verify locally first.
